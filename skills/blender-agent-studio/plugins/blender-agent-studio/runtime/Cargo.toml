[package]
name = "bas-runtime"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Bounded SceneIR analysis for Blender Agent Studio"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
